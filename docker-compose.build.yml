# For Vigil development: build the image from this checkout instead of pulling a release.
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  vigil:
    image: vigil:local
    build:
      context: .
      args:
        VIGIL_VERSION: dev
